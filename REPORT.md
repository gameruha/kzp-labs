# Звіт до лабораторної роботи № 1

## 1. Тема, номер і варіант

- Тема: розгортання Java-проєкту та базова обробка даних.
- Номер роботи: 1.
- Варіант: 12, «Розсадник рослин».
- Операційна система: Windows 11.
- GitHub-репозиторій: https://github.com/gameruha/kzp-labs

## 2. Мета роботи

Створено Maven-проєкт, який читає UTF-8 CSV, перевіряє записи рослин, обчислює
чотири показники варіанта та формує консольний і файловий звіт.

## 3. Постановка задачі

Формат запису: `species:String; name:String; heightCm:double; price:double; wateringDays:int`.
Показники: кількість коректних записів, середня висота, найдорожча рослина,
найменший інтервал поливу. Некоректні рядки не зупиняють програму та містять номер
рядка і причину помилки.

## 4. Структура програми

- `Main` розбирає аргументи, читає UTF-8, валідовує рядки, обчислює показники та формує звіт.
- `Main.Plant` є записом одного коректного рядка.
- `Main.Analysis` зберігає коректні записи й помилки перевірки.
- `data/input.csv` містить заголовок, три коректні та два некоректні приклади.
- `MainTest` перевіряє метрики, український текст, помилки, номери рядків і порожній набір.

Потік даних: аргументи → `Path` → `Files.readAllLines(..., UTF_8)` → `split(";", -1)`
→ перевірка → `Analysis` → один текст звіту → консоль і `out/report.txt`.

## 5. Інфраструктура

У `pom.xml` налаштовано Java 21, JUnit Jupiter, SpotBugs у фазі `verify` та Maven
Shade Plugin у фазі `package` з main-класом `ua.lpnu.kzp.Main`. Maven Wrapper зберігає
версію Maven у `.mvn/wrapper/`. Workflow використовує JDK 21, кеш Maven, матрицю
Ubuntu/Windows/macOS, `verify`, `package` та `upload-artifact`.

## 6. GitHub Issues і Pull Request

У репозиторії створено Issues для основних частин роботи:

- [Issue #1: Implement core data parsing and validation](https://github.com/gameruha/kzp-labs/issues/1)
- [Issue #2: Add business logic and calculations for Variant 12](https://github.com/gameruha/kzp-labs/issues/2)
- [Issue #3: Setup unit tests and GitHub Actions CI workflow](https://github.com/gameruha/kzp-labs/issues/3)
- [Pull Request #5: Add GitHub Actions workflow for multi-OS CI](https://github.com/gameruha/kzp-labs/pull/5)

| Issue | Зміна | Коміт або PR |
|---|---|---|
| #3 | Maven, Wrapper, SpotBugs, Shade, CI | [PR #5](https://github.com/gameruha/kzp-labs/pull/5) |
| #1 | Парсер і валідація варіанта 12 | [commit b46e09d](https://github.com/gameruha/kzp-labs/commit/b46e09de38c1fd2a528804247a94c2ced449f3d9) |
| #2 | Показники та звіт варіанта 12 | [commit b46e09d](https://github.com/gameruha/kzp-labs/commit/b46e09de38c1fd2a528804247a94c2ced449f3d9) |

## 7. Приклад роботи

Команда `java -jar target/lab01-1.0.0.jar --input data/input.csv --output out/report.txt`
обробляє три коректні записи. Порожнє ім'я та текст у числовому полі відкидаються
з повідомленнями на кшталт `Рядок 3: species і name не можуть бути порожніми`.
Звіт створюється в `out/report.txt` у UTF-8.

## 8. Тестування

Локально виконано:

```text
.\mvnw.cmd test
.\mvnw.cmd verify
```

Результат: 4 тести пройшли, SpotBugs повідомив про 0 помилок, JAR створюється.
Успішний запуск Actions: [Java CI run #4](https://github.com/gameruha/kzp-labs/actions/runs/35627370573).
Артефакти цього запуску: `jar-ubuntu-latest-4`, `jar-windows-latest-4`, `jar-macos-latest-4`.

## 9. Документація

Javadoc додано до публічного класу, `main`, методів аналізу, форматування та запису
звіту, а також до публічних записів `Plant` і `Analysis`.

## 10. Академічна доброчесність

Використано GitHub Copilot у VS Code як консультанта й помічника з перевірки.

- Внесок автора: визначення варіанта, перевірка вимог методички, перевірка запусків і результатів.
- Внесок ШІ: аудит репозиторію, пропозиції щодо структури, тестів, CLI та документації.
- Ролі: DevOps, Розробник, Валідатор, Документатор і Рецензент описані в `ai/`.
- Прийняті рекомендації перевірені командами Maven і тестами; знайдені помилки виправлені.
- Автор відповідає за розуміння та остаточну перевірку всього поданого коду.

## 11. Відповіді на контрольні питання

1. `pom.xml` описує координати, Java, залежності та Maven-плагіни проєкту.
2. `test` компілює і запускає тести, `verify` додає перевірки якості, `package` створює JAR.
3. Wrapper забезпечує однакову версію Maven без глобальної установки.
4. `main` є точкою входу консольної програми.
5. Примітив зберігає значення, `String` є посилальним об'єктом тексту.
6. `double` зберігає дробову частину середнього.
7. `Integer.parseInt("abc")` кидає `NumberFormatException`.
8. Без повідомлення неможливо перевірити якість вхідних даних і знайти дефект.
9. `split(";", -1)` зберігає порожнє останнє поле.
10. `Path.of` працює з роздільниками різних операційних систем.
11. Явний UTF-8 однаково обробляє українські символи на різних ОС.
12. `%n` використовує роздільник рядків поточної ОС, а `\n` є фіксованим символом.
13. Перевіряються п'ять полів, порожні тексти, формат чисел і допустимі межі.
14. Тест із двома висотами та очікуваним дробовим середнім виявляє помилку ділення.
15. Статичний аналізатор шукає типові дефекти у скомпільованому коді.
16. GitHub Actions автоматично запускає однакові перевірки на трьох ОС.
17. Issue дефекту має містити кроки, очікуваний і фактичний результат.
18. Треба вказати інструмент ШІ, ролі, запити, внесок автора, перевірки та відповідальність.
19. DevOps відповідає за інфраструктуру, Валідатор за крайові випадки й дефекти.
20. Найскладніший фрагмент: послідовність валідації, бо вона не повинна змінювати статистику.

## 12. Висновки

Створено кросплатформну консольну програму для варіанта 12 із Maven-збіркою,
тестами, статичним аналізом, виконуваним JAR і CI. У лабораторній роботі № 2
рядкові записи можна замінити предметними класами, не змінюючи CLI та формат звіту.

## Версіювання рівня 3

- Версія в `pom.xml` і `--version`: `1.0.0`.
- Git-тег: [`v1.0.0`](https://github.com/gameruha/kzp-labs/releases/tag/v1.0.0).
- Workflow: [успішний запуск Java CI](https://github.com/gameruha/kzp-labs/actions/runs/35627370573).
- Артефакти: три JAR для Ubuntu, Windows і macOS доступні на сторінці запуску workflow.